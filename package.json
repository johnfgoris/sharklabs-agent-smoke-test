{
  "name": "sharklabs-agent-smoke-test",
  "version": "1.0.0",
  "private": true,
  "description": "Small disposable public repo used to smoke-test agent containers (PLATFORM-038). Not a real application.",
  "scripts": {
    "test": "node --test *.test.mjs scripts/*.test.mjs"
  }
}
